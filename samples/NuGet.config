<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <add key="nuget.org" value="https://www.nuget.org/api/v2/" />

    <!-- This is a private feed that requires a username and password to access.

         Before using this feed, you'll need to configure your local machine with the API key
         and provide a user name and password to connect to myget.org

         nuget setapikey [apikey] -source [feedUrl]
         nuget sources add|update -Name [name] -source [feedUrl] -User [username] -pass [password]

         For more information please see http://docs.myget.org/docs/walkthrough/getting-started-with-nuget
         -->
    <add key="MyGet dotnet-xunit-performance" value="https://www.myget.org/f/dotnet-xunit-performance/api/v2" />
  </packageSources>
</configuration>